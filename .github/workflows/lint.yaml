name: OpenTofu CI

on:
  pull_request:
    branches: [main]
    paths:
      - '**/*.tf'
      - '**/*.yaml'
      - '**/*.yml'

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup OpenTofu
        uses: opentofu/setup-opentofu@v1
        with:
          tofu_version: '1.8.0'

      - name: OpenTofu Format Check
        run: |
          if ! tofu fmt -check -recursive > /dev/null 2>&1; then
            echo "Formatting issues found:"
            echo ""
            tofu fmt -diff -recursive
            echo ""
            echo "Run 'tofu fmt -recursive' locally to fix these issues."
            exit 1
          fi
          echo "All files formatted correctly."

      - name: OpenTofu Validate
        run: |
          # Find all directories with .tf files and validate each
          DIRS=$(find . -type f -name "*.tf" -not -path "*/.terraform/*" -exec dirname {} \; | sort -u)
          EXIT_CODE=0
          for dir in $DIRS; do
            echo "Validating: $dir"
            cd "$dir"
            tofu init -backend=false
            tofu validate || EXIT_CODE=1
            cd - > /dev/null
          done
          if [ $EXIT_CODE -ne 0 ]; then
            echo "Validation failed"
            exit 1
          fi
          echo "All modules valid"

      - name: Install yamllint
        run: pip install yamllint

      - name: YAML Lint
        run: yamllint -d relaxed .
